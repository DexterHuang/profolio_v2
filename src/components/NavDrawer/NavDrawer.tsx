
import * as React from "react";
import { Color } from "../../theme/Color";

interface Props {

}
export const NavDrawer: React.FC<Props> = (props: Props) => {
    const { } = props;
    return (
        <div style={{ height: "100%", width: 180, backgroundColor: Color.PRIMARY_DARK }}>
        </div>
    );
};
