
import * as React from "react";
import { Color } from "../../theme/Color";

interface Props {

}
export const NavBar: React.FC<Props> = (props: Props) => {
    const { } = props;
    return (
        <div style={{ width: 40, height: "100%", backgroundColor: Color.PRIMARY_MEDIUM }}>

        </div>
    );
};
