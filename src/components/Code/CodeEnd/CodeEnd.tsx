import * as React from "react";
import { Text } from "../../Text/Text";
interface Props { }
export const CodeEnd: React.FC<Props> = (props: Props) => {
  const { } = props;
  return (
    <Text type="symbol" style={{ marginLeft: -4 }}>
      ;
    </Text>
  );
};
